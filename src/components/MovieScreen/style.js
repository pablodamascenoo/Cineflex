import styled from "styled-components";

export const BoxMovies = styled.div`
  display: flex;
  flex-wrap: wrap;
  gap: 30px;
  width: calc(100% - 60px);
  margin: auto;
  justify-content: center;
`;
